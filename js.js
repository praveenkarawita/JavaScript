//convert to int
function convertToInteger(str){
    return parseInt (str);

    "use strict";
}

console.log(convertToInteger("345"));

// if we declare varibale with const we cant mutate it but we can mutate const arrays and objects as usal ways 

//import and export

// need to add below in import file/class


import { capitalizeString } from "./capitalizeString";

const cap = capitalizeString ("Hello!");

console.log(cap);

